trigger:
  branches:
    include:
    - main
  paths:
    include:
      - environments/dev
variables:
  - group: remote_state



pool:
  name: ggagents
stages:
- stage: BuildStage
  jobs:
  - job: TerraformInitialization
    displayName: Terraform Initialization
    steps:
    - task: TerraformInstaller@1
      displayName: Terraform Install
      inputs:
        terraformVersion: 'latest'
    - task: TerraformTask@5
      displayName: Terraform Init
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: '$(System.DefaultWorkingDirectory)/environments/dev'
        backendServiceArm: 'Appserviceconn'
        backendAzureRmResourceGroupName: $(rg_name)
        backendAzureRmStorageAccountName: $(stg_name)
        backendAzureRmContainerName: $(container_name)
        backendAzureRmKey: 'devapstate.tf'
    - task: TerraformTask@5
      displayName: Terraform Plan
      inputs:
        provider: 'azurerm'
        command: 'plan'
        workingDirectory: '$(System.DefaultWorkingDirectory)/environments/dev'
        environmentServiceNameAzureRM: 'Appserviceconn'
- stage: DeployStage
  dependsOn:
  - BuildStage
  jobs:
  - job: ManualValidation
    displayName: Manual Validation
    pool:
      name: server
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    steps:
    - task: ManualValidation@1
      inputs:
        notifyUsers: 'gauravgupta.id1@gmail.com'
        onTimeout: 'resume'
  - job: TerraformApply
    dependsOn:
    - ManualValidation
    pool:
      name: ggagents
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    steps:
    - task: TerraformInstaller@1
      inputs:
        terraformVersion: 'latest'
    - task: TerraformTask@5
      displayName: Terraform Init
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: '$(System.DefaultWorkingDirectory)/environments/dev'
        backendServiceArm: 'Appserviceconn'
        backendAzureRmResourceGroupName: 'Apbackend'
        backendAzureRmStorageAccountName: 'ggstginfra'
        backendAzureRmContainerName: 'backend'
        backendAzureRmKey: 'devapstate.tf'
    - task: TerraformTask@5
      displayName: Terraform Apply
      inputs:
        provider: 'azurerm'
        command: 'apply'
        workingDirectory: '$(System.DefaultWorkingDirectory)/environments/dev'
        environmentServiceNameAzureRM: 'Appserviceconn'
